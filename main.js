define( [ 'readium_js_plugins', 'jquery' ], function ( Plugins, $ ) {

    Plugins.register( 'dltsRjsPluginOaBooks', function ( api ) {

        api.plugin.warn( 'Registered dltsRjsPluginOaBooks' );

    } );

} );
